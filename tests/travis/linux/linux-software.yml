---

skipped_roles:
  - homebrew     # FIXME (homebrew module doesn't find path to linuxbrew's brew)
  - openzfs      # disabled until we fix the held broken packages on apt-get install.
  - sublimetext  # not implemented yet

apt:
  upgrade_all: no
  packages:
    - htop

homebrew:
  packages:
    - tree

rvm:
  rubies:
    - ruby-1.9.3
    - 2.1
    - ruby
    - rbx
    - jruby

