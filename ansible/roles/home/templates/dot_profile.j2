
{% if ansible_system == 'Linux' %}
# FIXME: only when .linuxbrew exists
#
# Load `brew` in Linux
#
export PATH="$HOME/.linuxbrew/bin:$PATH"
export MANPATH="$HOME/.linuxbrew/share/man:$MANPATH"
export INFOPATH="$HOME/.linuxbrew/share/info:$INFOPATH"
{% endif %}

#
# Load RVM
#
#[[ -d "$HOME/.rvm" ]] && export PATH="$PATH:$HOME/.rvm/bin"          # Add RVM to PATH for scripting
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*

#
# alias hub to git for best feelings!
# see https://github.com/github/hub#aliasing
#
# Disable for now, since it breaks git completion in zsh
# See https://github.com/github/hub/issues/586#issuecomment-47727226
#if [ -x /usr/local/bin/hub ]; then
#  eval "$(hub alias -s)"
#fi

# TODO git aliases

#
# FIXME: only export this var when vagrant is installed and
# when "wanted"...
#
export VAGRANT_DOTFILE_PATH=.vagrant_"$(hostname -s)"

#
# More User stuff ?
#
export PATH=$PATH:$HOME/bin